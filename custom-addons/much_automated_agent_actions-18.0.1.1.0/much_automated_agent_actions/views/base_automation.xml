<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="view_base_automation_search" model="ir.ui.view">
        <field name="name">base.automation.search</field>
        <field name="model">base.automation</field>
        <field name="inherit_id" ref="base_automation.view_base_automation_search" />
        <field name="arch" type="xml">
           <filter name="inactive" position="before">
               <filter
                    string="Generative AI"
                    name="generative_ai"
                    domain="[('action_server_ids.state', '=', 'generative_ai')]"
                />
           </filter>
        </field>
    </record>

    <record id="base_automation_window" model="ir.actions.act_window">
        <field name="name">Automation Rules</field>
        <field name="res_model">base.automation</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="view_id" ref="base_automation.view_base_automation_kanban" />
        <field
            name="context"
        >{'active_test': False, 'search_default_generative_ai': True}</field>
        <field name="help" type="html">
            <img
                class="w-100 w-md-75"
                src="/base_automation/static/img/automation.svg"
            />
            <p>
                Automate <em>everything</em> with Automation Rules
            </p>
            <p>
                Send an email when an object changes state, archive records
                after a month of inactivity or remind yourself to follow-up on
                tasks when a specific tag is added.
            </p>
            <p>With Automation Rules, you can automate
                <em>any</em>
                workflow.
            </p>
        </field>
    </record>

    <menuitem
        id="base_automation_menu"
        parent="ai_actions_menu"
        action="base_automation_window"
        sequence="10"
    />
</odoo>
